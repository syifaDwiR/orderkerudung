<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.toshiba.orderkerudung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/frada"
        android:label="frada.id"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.example.toshiba.orderkerudung.SplashScreen">
            android:theme="@style/Splashscreen"
            android:screenOrientation="portrait"
            android:noHistory="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".OrderActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".TopMenuActivity" />
        <activity android:name=".milodiol" />
        <activity android:name=".dark_flower"></activity>
        <activity android:name=".pilpink" />
        <activity android:name=".polos" />
        <activity android:name=".maura">

        </activity>

    </application>

</manifest>